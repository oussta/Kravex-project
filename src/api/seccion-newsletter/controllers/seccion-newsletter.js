'use strict';

/**
 * seccion-newsletter controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::seccion-newsletter.seccion-newsletter');
