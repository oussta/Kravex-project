[phases.setup]
nixPkgs = ["nodejs_20", "python3", "make", "gcc"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build -- --debug"]

[start]
cmd = "npm run start"